using UnityEngine;
using System.Collections;

public class FallerScript : GameController {

	// Use this for initialization
	void Start () 
    {
        //Get colors from the blocks
        //Must be the same colors as the blocks
        int colorIndex = Random.Range(0, blockcolors.Length);
	}
	
	// Update is called once per frame
	void Update () 
    {

	}
}
